feat(T7): implement transfer between accounts with atomic pair creation

- Add create_transfer_pair SQL RPC that atomically inserts both debit and
  credit legs, bi-directionally links them via transfer_pair_id, and
  validates account ownership through RLS (security invoker)
- Add to_account_id to CreateTransactionSchema with superRefine ensuring
  from/to accounts differ
- Branch createTransaction into a transfer path that pre-computes FX for
  both legs (cross-currency supported via convertToBase), calls the RPC,
  and revalidates both account paths
- Extend deleteTransaction to soft-delete the paired leg together so both
  account balances update atomically; bulkDeleteTransactions likewise
  auto-expands to include transfer pair rows
- Quick-add form shows Sa racuna / Na racun selects for transfer kind,
  hides merchant and category fields, and shows a hint for cross-currency
- ConfirmDeleteDialog description flags when a transfer pair will be deleted
- Display of Transfer: A -> B works automatically via transfer_pair_id and
  transfer_counterparty_account_name (implemented in T8)
